% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/react_ae_listing.R
\name{react_ae_listing}
\alias{react_ae_listing}
\title{Interactive AE listing table}
\usage{
react_ae_listing(
  outdata,
  default_page_size = 15,
  searchable = TRUE,
  striped = TRUE,
  highlight = TRUE,
  patient_folding = FALSE
)
}
\arguments{
\item{outdata}{An \code{outdata} object created by \code{\link[=format_ae_listing]{format_ae_listing()}}.}

\item{default_page_size}{Number of rows to display per page.}

\item{searchable}{A boolean value to enable global search. The default is TRUE.}

\item{striped}{A boolean value to display striped rows. The default is TRUE.}

\item{highlight}{A boolean value to highlight row on hover. The default is TRUE.}

\item{patient_folding}{A boolean value to control patient-level folding. The default is FALSE.
If \code{TRUE}, all rows are hidden by default and only the first column
(patient ID column) is filterable; other column filters are disabled.
In this mode, records are shown only when the entered value exactly matches
a full patient ID in the first column.
If \code{FALSE}, all rows are displayed and filters are available for all columns.}
}
\value{
A \code{reactable} htmlwidget object.
}
\description{
Interactive AE listing table
}
\examples{
if (requireNamespace("reactable", quietly = TRUE)) {
  meta <- meta_ae_test()
  meta |>
    prepare_ae_listing(
      analysis = "ae_listing",
      population = "apat",
      observation = "wk12",
      parameter = "rel"
    ) |>
    format_ae_listing() |>
    react_ae_listing()
}
}
